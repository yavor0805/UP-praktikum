#include <cmath>
#include <iostream>
using namespace std;
int main()
{
    int day = 0, month = 0, year = 0, totalDays = 0;
    cout << " Insert date: ";
    cin >> day >> month >> year;

    totalDays = totalDays + day;
    if (year % 4 == 0 || year % 400 == 0)
    {
        if (year % 100 == 0)
        {
           
        }
            
        else 
        {
            //leap
        }
    }
   
    




}